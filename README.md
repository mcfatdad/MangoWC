# MangoWC split config

This bundle was generated by splitting your existing `config.conf` into a modular structure.

## Layout

- `config.conf` – tiny loader that `source=`s everything under `conf.d/`
- `conf.d/*.conf` – core settings
- `conf.d/binds/*.conf` – keybinds / mousebinds / axisbinds

## Install

Copy this folder to:

`~/.config/mango/`

Then reload MangoWC (your config already binds a reload key).

## Notes

- `conf.d/99-local.conf` is intended for machine-specific overrides.
- `autostart.sh` and `settings.file` are placeholders in this bundle (copy your real ones over).

Generated: 2026-01-13 17:55:57Z (UTC)
