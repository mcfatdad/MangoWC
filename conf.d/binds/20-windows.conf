# Split from original config.conf (2026-01-13 17:55:57Z UTC)
# Original: config.conf


# =============================
# Windows (from windows.conf)
# =============================
bind=SUPER,q,killclient

bind=SUPER,f,togglefullscreen
bind=SUPER,m,togglemaximizescreen
bind=SUPER,t,togglefloating
bind=SUPER+SHIFT,t,spawn_shell,mango-run float-all-windows
#bind=SUPER+SHIFT,z,spawn_shell,${XDG_CONFIG_HOME:-$HOME/.config}/mango/scripts/toggle-all-floating

# Focus movement
bind=SUPER,Left,focusdir,left
bind=SUPER,Right,focusdir,right
bind=SUPER,Up,focusdir,up
bind=SUPER,Down,focusdir,down

# Swap tiled windows
bind=SUPER+SHIFT,Left,exchange_client,left
bind=SUPER+SHIFT,Right,exchange_client,right
bind=SUPER+SHIFT,Up,exchange_client,up
bind=SUPER+SHIFT,Down,exchange_client,down

# Mouse wheel bindings use axisbind, not mousebind.
# Tag switching by wheel is configured in conf.d/binds/40-tags.conf.

# Drag floating windows (grab anywhere in the window)
mousebind=SUPER,btn_left,moveresize,curmove
mousebind=SUPER,btn_right,moveresize,curresize

# Resize floating window in small steps (width,height)
# (negative shrinks, positive grows)
bind=SUPER+CTRL,Left,resizewin,-40,0
bind=SUPER+CTRL,Right,resizewin,+40,0
bind=SUPER+CTRL,Up,resizewin,0,-40
bind=SUPER+CTRL,Down,resizewin,0,+40

# Smart resize floating window (auto sizing)
#bind=SUPER,Left,smartresizewin,left
#bind=SUPER,Right,smartresizewin,right
#bind=SUPER,Up,smartresizewin,up
#bind=SUPER,Down,smartresizewin,down

# Alt+Tab cycling
bind=ALT,Tab,focusstack,next
bind=ALT+SHIFT,Tab,focusstack,prev

# Manage Masters
# The current MangoWM parser does not accept literal + or - as key names.
# Leave these disabled until the exact keysyms are confirmed with `wev`.
# bind=SUPER,plus,incnmaster,+1
# bind=SUPER,minus,incnmaster,-1


# ------------------------------------------------------------
